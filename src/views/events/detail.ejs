<%- include('../partials/header') %> <main class="container">
    <article class="event-detail-page">
        <header>
            <h1><%= event.title %></h1>
            <span class="badge"><%= event.category %></span>
        </header>

        <section class="event-meta">
            <p><strong>Location:</strong> <%= event.location %></p>
            <p><strong>Date/Time:</strong> <%= event.startDatetime %></p>
            <p><strong>Capacity:</strong> <%= event.capacity ? event.capacity : "No limit" %></p>
        </section>

        <section class="event-body">
            <h3>Description</h3>
            <p><%= event.description %></p>
        </section>

        <hr>

        <div class="user-actions">
            <% /* Sprint 1 Logic: Only show Admin/Organizer buttons if they own the event */ %>
            <% if (user && (user.id === event.organizerId || user.role === 'admin')) { %>
                <div class="admin-panel">
                    <p><em>Organizer View</em></p>
                    <a href="/events/<%= event.id %>/edit" class="btn btn-edit">Edit Event</a>
                    <button class="btn btn-cancel">Cancel Event</button>
                </div>
            <% } else { %>
                <button class="btn btn-rsvp">RSVP for this Event</button>
            <% } %>
        </div>
    </article>
</main>

<%- include('../partials/footer') %>